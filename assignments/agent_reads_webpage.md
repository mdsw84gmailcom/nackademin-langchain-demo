## Agent That Reads a Webpage

## Implementation:
A web tool was added to the agent using RequestToolkit  and TextRequestWrapper from langchain_community. This allows the agent to retrieve content from a webpage and process it.

## Result:
The agent successfully fetched the content of hhtp://example.com and summarized the page. This show that the agent can use a web tool to access and analyze online information.