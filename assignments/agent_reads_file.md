# Agent That Reads a File

## Implementation
A file-reading tool was added to the agent. The tool reads the content of a local file from disk and returns it to the agent.

## Result:
The agent can now acsess and read files on the computer when the user asks for it. This makes it possible to use local documents as input in an agent workflow.